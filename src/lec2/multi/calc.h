#ifndef CALC_H
#define CALC_H

// Basic arithmetic and small utilities used across files.

int add(int a, int b);

// Returns n-th Fibonacci number (n >= 0), iterative implementation.
unsigned long long fib(int n);

#endif // CALC_H
